<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.changji.cloud.user.mapper.UsersMapper">


    <sql id="all_columns">
        user_id, account, institute, specialty, classes, real_name, nick_name, picture, status
    </sql>

    <insert id="save">
        insert into users (user_id, account, institute, specialty, classes, real_name) values
        (#{user.userId}, #{user.account}, #{user.institute}, #{user.specialty}, #{user.classes}, #{user.realName})
    </insert>
</mapper>
