<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.changji.cloud.social.mapper.UserRelationshipMapper">

    <sql id="all_columns">
        id, user_account, friend_account, rel_state, alias_user,alias_friend, create_time, update_time
    </sql>
    <select id="selectRelationship" resultType="com.changji.cloud.social.model.UserRelationship">
        select * from user_relationship where rel_state= 1 and (user_account= #{userAccount} and friend_account=#{friendAccount})
    </select>

    <select id="selectFriends" resultType="com.changji.cloud.social.model.UserRelationship">
        select * from user_relationship where user_account=#{userAccount} and status=1
    </select>


</mapper>
